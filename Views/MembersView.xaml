<UserControl x:Class="CloudMining.Views.MembersView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:vm="clr-namespace:CloudMining.ViewModels"
			 xmlns:local="clr-namespace:CloudMining.Views"
			 mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="800">

	<UserControl.DataContext>
		<vm:MembersViewModel/>
	</UserControl.DataContext>

	<Grid>
		<DataGrid ItemsSource="{Binding MembersList}" IsReadOnly="True" AutoGenerateColumns="False">
			<DataGrid.ContextMenu>
				<ContextMenu>
					<MenuItem Header="Добавить участника" Command="{Binding AddNewMemberCommand}"/>
				</ContextMenu>
			</DataGrid.ContextMenu>

			<DataGrid.Columns>
				<DataGridTextColumn Header="id" Binding="{Binding id}"/>
				<DataGridTextColumn Header="Имя" Binding="{Binding name}"/>
				<DataGridTextColumn Header="Роль" Binding="{Binding Role.name}"/>
				<DataGridTextColumn Header="Дата вступления" Binding="{Binding joinDate}"/>
				<DataGridTextColumn Header="Сумма пополнений" Binding="{Binding Deposits}"/>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>
