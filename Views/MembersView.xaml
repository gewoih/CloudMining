<UserControl x:Class="CloudMining.Views.MembersView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:vm="clr-namespace:CloudMining.ViewModels"
			 xmlns:local="clr-namespace:CloudMining.Views"
			 xmlns:c="clr-namespace:CloudMining.Data"
			 mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="800">

	<UserControl.DataContext>
		<vm:MembersViewModel/>
	</UserControl.DataContext>

	<Grid>
		<DataGrid ItemsSource="{Binding MembersList}" SelectedItem="{Binding SelectedMember}" IsReadOnly="True" AutoGenerateColumns="False">
			<DataGrid.ContextMenu>
				<ContextMenu>
					<MenuItem Header="Добавить участника" Command="{Binding AddNewMemberCommand}"/>
					<MenuItem Header="Удалить участника" Command="{Binding DeleteMemberCommand}"/>
				</ContextMenu>
			</DataGrid.ContextMenu>

			<DataGrid.Columns>
				<DataGridTextColumn Header="id" Binding="{Binding Id}"/>
				<DataGridTextColumn Header="Имя" Binding="{Binding Name}"/>
				<DataGridTextColumn Header="Роль" Binding="{Binding Role.Name}"/>
				<DataGridTextColumn Header="Дата вступления" Binding="{Binding JoinDate}"/>
				<DataGridTextColumn Header="Сумма пополнений" Binding="{Binding Deposits.Count}"/>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>
