<UserControl x:Class="CloudMining.Views.InvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CloudMining.Views"
			 xmlns:vm="clr-namespace:CloudMining.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

	<UserControl.DataContext>
		<vm:InvoicesViewModel/>
	</UserControl.DataContext>
	
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="10*"/>
		</Grid.RowDefinitions>

		<Menu Grid.Row="0" Background="#031C4E" Foreground="#ACC1EA">
			<MenuItem Header="Выплаты" Command="{Binding ChangeMenuIndexCommand}" CommandParameter="1"/>
			<MenuItem Header="Покупки" Command="{Binding ChangeMenuIndexCommand}" CommandParameter="2"/>
			<MenuItem Header="Электричество" Command="{Binding ChangeMenuIndexCommand}" CommandParameter="3"/>
		</Menu>

		<StackPanel Grid.Row="1">
			<ContentControl Content="{Binding MainContentControl}" MaxHeight="410"/>
		</StackPanel>
	</Grid>
</UserControl>
