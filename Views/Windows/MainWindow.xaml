<Window x:Class="CloudMining.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:vm="clr-namespace:CloudMining.ViewModels"	
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1024" Background="#2B467A">

	<Window.DataContext>
		<vm:MembersViewModel/>
	</Window.DataContext>
	
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="4*"/>
		</Grid.ColumnDefinitions>

		<ListView Background="#6E8FD1" Foreground="White" FontSize="24">
			<ListViewItem x:Name="membersMenu">
				<StackPanel Orientation="Horizontal">
					<TextBlock Text="Участники"/>
				</StackPanel>
			</ListViewItem>
			<MenuItem Header="x" Command="{Binding LoadMembersCommand}">
				
			</MenuItem>
		</ListView>

		<StackPanel x:Name="MainPanel" Grid.Row="0" Grid.Column="1">
			<DataGrid ItemsSource="{Binding MembersList}" AutoGenerateColumns="True"/>
		</StackPanel>
	</Grid>
</Window>
